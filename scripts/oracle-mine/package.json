{
  "name": "muhaven-oracle-mine",
  "version": "0.1.0",
  "private": true,
  "description": "Scrapers for harvesting RWA reference data (oracle bootstrap) from app.rwa.xyz. Internal use only.",
  "type": "module",
  "scripts": {
    "scrape:asset": "tsx scripts/scrape-asset.ts",
    "extract:asset": "tsx scripts/extract-asset.ts",
    "fetch:timeseries": "tsx scripts/fetch-timeseries.ts",
    "refresh:all": "tsx scripts/refresh-all.ts",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
