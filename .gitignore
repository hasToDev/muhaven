test_snapshots/

# Deployment info
deployment.json

# Environment
.env
.env.local
.env.stage
.env.stage.local

# Dependencies
node_modules/
bun.lockb
# Root project uses pnpm — a stray `bun install` at root drops a `bun.lock`
# that should never be committed. Frontend's `frontend/bun.lock` remains
# tracked because the frontend package uses bun.
/bun.lock

# Hardhat artifacts
artifacts/
cache/
typechain-types/
coverage/

# Phase 9.A · Expansion (F2) — staged-by-deploy-homelab contract bytecode
# for the self-serve issuer onboarding deploy library. Generated from
# `artifacts/contracts/` at staging deploy time; not source.
backend/contracts-artifacts/contracts/
coverage.json
deployments/localcofhe/
deployments/localcofhe.json
deployments/hardhat.json
deployments/localhost.json
deployments/history/

# Frontend build
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

.claude/
.openzeppelin
CLAUDE.md
development/
*.bak

scripts/deploy-homelab.sh