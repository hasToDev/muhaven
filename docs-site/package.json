{
  "name": "@muhaven/docs-site",
  "version": "1.0.13",
  "private": true,
  "type": "module",
  "description": "User-facing documentation for MuHaven's confidential RWA portfolio platform — focused on the four agentic surfaces (HavenBot, MCP, OpenClaw, Hosted Checkout).",
  "scripts": {
    "dev": "vitepress dev --port 5174 --host 127.0.0.1",
    "build": "vitepress build",
    "preview": "vitepress preview --port 5174 --host 127.0.0.1",
    "deploy": "vitepress build && node scripts/deploy-to-muhaven-document-web.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.13"
  }
}
